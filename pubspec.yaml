name: kurier_web
description: Kurier Flutter web overlay
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  characters: ^1.4.0
  collection: ^1.19.0
  file_picker: ^10.3.2
  flutter_html: ^3.0.0
  flutter_riverpod: ^2.6.1
  flutter_webrtc: ^1.2.0
  http: ^1.5.0
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.2
  web: ^1.1.1
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
